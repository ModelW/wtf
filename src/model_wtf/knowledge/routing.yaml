# Model per `auto` stage, as OpenRouter ids (provider/model). The default is
# OpenRouter's auto router (price/quality pareto); repos override in
# `.model-wtf.yml#routing`, the CLI with `--model-override stage=model`.
default: openrouter/openrouter/auto
stages: {}
