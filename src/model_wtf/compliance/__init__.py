"""The ``model-wtf compliance`` command family.

Everything under this package is about checking a repository's compliance
declarations (the ``compliance/`` folders) against the rules Model W
expects. This first slice only discovers *where* those declarations live
and whether anything is declared at all.
"""
