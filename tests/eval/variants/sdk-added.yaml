apply:
- prepend: api/apps/leads/api.py
  text: 'import os


    import stripe


    stripe.api_key = os.environ["STRIPE_SECRET_KEY"]

    '
expect:
  recipients:
  - stripe
  findings:
  - GDPR-RECIPIENT-DECLARED
